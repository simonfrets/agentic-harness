# Managed by Agentic Harness.
#
# Everything ignored here is machine-local: a private dependency tree, and
# transcripts, locks and logs from individual runs. Rules, agent definitions
# and task state are deliberately *not* ignored, because a workflow that
# cannot be reviewed in a pull request is not governed by anything.

node_modules/
state/

*.log
*.tmp
*.lock
